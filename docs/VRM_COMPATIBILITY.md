# VRM compatibility

Target valid VRM 0.x / 1.x humanoid avatars whose source terms permit modification.

Fail cleanly for corrupt files, non-humanoid models, missing required bone mapping,
unsupported geometry/material cases, or terms that explicitly prohibit modification.
